<% if model.errors.any? %>
  <div class="alert alert-danger">
    <ul class="list-unstyled">
      <div class="h5">エラーが起こりました。</div>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>